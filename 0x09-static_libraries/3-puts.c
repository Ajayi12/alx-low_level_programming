#include "main.h"
#include <stdio.h>
/**
 * _puts - function that prints a string followed by new lines
 * @str: pointer character of the parameter
 * Return: Always success (0)
 */
void _puts(char *str)
{
	puts(str);
}
