#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string
 * @s: integer pointer value of the parameter
 * Return: Always 0 (success)
 */
int _strlen(char *s)
{
	int result;

	result = strlen(s);
	return (result);
}
