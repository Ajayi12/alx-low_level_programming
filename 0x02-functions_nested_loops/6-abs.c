#include "main.h"
#include <stdlib.h>
/**
 * _abs - program that computes the absolute value of an integer
 * @c: The input integer for which the absolute value is calculated
 * Return: always 0 (success)
 */
int _abs(int c)
{
	abs(c);
	return (c);
}
